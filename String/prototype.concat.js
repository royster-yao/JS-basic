// concat() 函数用于将当前字符串与指定字符串进行拼接，并返回拼接后的字符串。
// 语法：str.concat( string2, string3 [, ..., stringN] )

/**
 * * str.concat( string2, string3 [, ..., stringN] )
 * @param {string}: 和原字符串连接的多个字符串。
 * @returns: 其返回值为拼接后的字符串
 */

const str = "Hello"
const result = str.concat("World", "!")
console.log(result)
