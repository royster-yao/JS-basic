// codePointAt() 方法返回 一个 Unicode 编码点值的非负整数。
// 语法： str.codePointAt(index);

/**
 * * str.codePointAt(index)
 * @param {str}: 目标字符串
 * @param {index}: 	这个字符串中需要转码的元素的位置索引。
 * @returns: 返回值是在字符串中的给定索引的编码单元体现的数字，如果在索引处没找到元素则返回 undefined。
 */

console.log("AaBbCc".codePointAt(1))
