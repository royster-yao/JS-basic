// Array.prototype.sort() 方法用于将数组对象的成员按指定顺序进行排序，并返回排序后的数组。
// 语法：arr.sort(compareFn);

/**
 * * arr.sort(compareFn)
 * @param {arr}: 目标数组
 * @param {compareFn}: 	（可选）指定如何比较元素顺序的函数名称
 * @returns: 返回排序后的数组对象。
 */

const arr = ["b", "c", "a"]
arr.sort()
console.log(arr)
