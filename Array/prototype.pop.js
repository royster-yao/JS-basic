// Array.prototype.pop() 方法用于移除数组最后一个成员，并返回该数组成员。
// 语法：arr.pop();

/**
 * * arr.pop()
 * @param {arr}: 目标函数
 * @returns 返回被移除的数组成员。如果该数组为空（没有任何元素），则返回 undefined。
 */

const arr = [1, 2, 3, "a"]
arr.pop()
console.log(arr)
