// Array.prototype.push() 方法用于向当前数组的末尾添加一个或多个元素，并返回新的数组长度。
// 语法：arr.push( item1 [, items...] )

/**
 * * arr.push( item1 [, items...] )
 * @param {arr}: 目标数组
 * @param {item}: 添加元素
 * @returns: 返回添加元素后的数组长度。
 */

const arr = [1, 2, 3, 4, 5]
arr.push(6)
console.log(arr)
