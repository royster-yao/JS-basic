// Array.prototype.reverse() 方法用于将当前数组的成员顺序全部反转，并返回元素顺序反转后的数组。
// 语法：arr.reverse();

/**
 * * arr.reverse()
 * @param {arr}: 目标数组
 * @returns: 返回数组成员反转后的数组，
 */

const arr = [3, 2, 1]
arr.reverse()
console.log(arr)
